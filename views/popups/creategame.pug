h1 Create game
form(name="creategame",ng-controller="createGameController",ng-submit="submit(Layout.popup)",novalidate,style="align-items: center")
    nginput(
        ng-model="formData.gameName",
        name="gameName",
        placeholder="Game name",
        min="4",
        max="20",
        type="text",
        form="form"
    )
    nginput(
        ng-model="formData.password",
        name="password",
        placeholder="Password (Optional)",
        min="4",
        max="30",
        type="password",
        form="form"
    )
    section.field-center
        input(type="submit",ng-class="{'disabled' : creategame.gameName.$invalid}",ng-disabled="creategame.gameName.$invalid",value="Create")
        button.red(ng-click="Layout.popup.changeState(false);cleanUp()",type="button") Cancel
